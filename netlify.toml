[build]
  command = "rm -rf node_modules package-lock.json && npm install --include=optional && npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
